<Window x:Class="_20.BindingTest1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_20.BindingTest1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="30,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
            <TextBlock Text="{Binding StringArrayWrapper.Strings[1]}"/>
            <TextBlock Text="{Binding StringArrayWrapper.Strings[2]}"/>
            <TextBlock Text="{Binding StrTestClass.TestStr}"/>
            <Button Content="Button" Width="100" Height="49" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
